// import { createSlice } from "@reduxjs/toolkit";

// const countSlice=createSlice({
//     name:'count',
//     initialState:[],
//     reducers:{
//         // addCount(state,action){return state<10? state+1:state},
//         // minCount(state,action){return state>0?state-1:state}
//         addUsers:(state,action)=>{ state.push('Mike','Ann','John','Rose','William')},
//         removeUser:(state,action)=>{return state.toSpliced(state,1)}
//     }
// });

// // export const {addCount} = countSlice.actions
// // export const {minCount} = countSlice.actions
// export const {addUsers} = countSlice.actions
// export const {removeUser} =countSlice.actions
// export default countSlice.reducer